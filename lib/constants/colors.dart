import 'package:flutter/material.dart';

/// Constantes de couleurs pour l'application MatchUp
class AppColors {
  // Couleurs principales
  static const Color primary = Color(0xFFFF3B3B);
  static const Color primaryPressed = Color(0xFFE23333);
  
  // Couleurs de fond
  static const Color background = Color(0xFFFFF5F2);
  static const Color card = Color(0xFFFFFFFF);
  
  // Couleurs de texte
  static const Color text = Color(0xFF1F2937);
  static const Color muted = Color(0xFF6B7280);
  
  // Empêcher l'instanciation de cette classe
  AppColors._();
}
